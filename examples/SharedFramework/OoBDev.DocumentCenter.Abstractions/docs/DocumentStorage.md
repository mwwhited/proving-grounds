# Document Center - Document Storage

## Summary

Document storage will be used for container/key persistence and retrieval of documents.  If a file name is not provided the system will generate a random file name.  The system will also tru to detect content type based on MIME type and file extension.  

See `IDocumentStore`.