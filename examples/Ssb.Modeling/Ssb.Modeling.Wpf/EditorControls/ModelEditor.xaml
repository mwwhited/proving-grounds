<UserControl x:Class="Ssb.Modeling.Wpf.EditorControls.ModelEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                     
            xmlns:m="clr-namespace:Ssb.Modeling.Models;assembly=Ssb.Modeling.Models"
            xmlns:md="clr-namespace:Ssb.Modeling.Wpf.DesignerData;assembly=Ssb.Modeling.Wpf"
            xmlns:c="clr-namespace:Ssb.Modeling.Wpf.Converters"
            xmlns:ed="clr-namespace:Ssb.Modeling.Wpf.EditorControls"
             
            x:Name="_root"
            mc:Ignorable="d" 
            d:DesignHeight="300" d:DesignWidth="300">
    <ContentControl Content="{Binding}">
        <ContentControl.Resources>
            <DataTemplate DataType="{x:Type m:ProjectModel}">
                <ed:ProjectEditor />
            </DataTemplate>
            <DataTemplate DataType="{x:Type m:ConversationGroupModel}">
                <ed:ConversationGroupEditor ConversationsSource="{Binding ElementName=_root,Path=ProjectContext.Conversations,Mode=OneWay}" />
            </DataTemplate>
            <DataTemplate DataType="{x:Type m:ConversationModel}">
                <ed:ConversationEditor ServicesSource="{Binding ElementName=_root,Path=ProjectContext.Services,Mode=OneWay}" />
            </DataTemplate>
            <DataTemplate DataType="{x:Type m:ServiceModel}">
                <ed:ServiceEditor ContractsSource="{Binding ElementName=_root,Path=ProjectContext.Contract,Mode=OneWay}" 
                                  QueuesSource="{Binding ElementName=_root,Path=ProjectContext.Queues,Mode=OneWay}" />
            </DataTemplate>
            <DataTemplate DataType="{x:Type m:QueueModel}">
                <ed:QueueEditor  />
            </DataTemplate>
            <DataTemplate DataType="{x:Type m:ContractModel}">
                <ed:ContractEditor MessageTypesSource="{Binding ElementName=_root,Path=ProjectContext.MessageTypes,Mode=OneWay}" />
            </DataTemplate>
            <DataTemplate DataType="{x:Type m:MessageTypeModel}">
                <ed:MessageTypeEditor XmlSchemaCollectionsSource="{Binding ElementName=_root,Path=ProjectContext.XmlSchemaCollections,Mode=OneWay}" />
            </DataTemplate>
            <DataTemplate DataType="{x:Type m:XmlSchemaCollectionModel}">
                <ed:XmlSchemaCollectionEditor />
            </DataTemplate>
        </ContentControl.Resources>
    </ContentControl>
</UserControl>
