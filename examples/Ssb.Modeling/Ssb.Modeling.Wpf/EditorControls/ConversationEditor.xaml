<UserControl x:Class="Ssb.Modeling.Wpf.EditorControls.ConversationEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:m="clr-namespace:Ssb.Modeling.Models;assembly=Ssb.Modeling.Models"
             mc:Ignorable="d" 
             x:Name="root"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Icons.xaml" />
                <ResourceDictionary Source="../Resources/EnumLists.xaml" />
                <ResourceDictionary Source="../Resources/TypeConverters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Conversation Name: " />
        <TextBox Grid.Column="1" Text="{Binding Path=ConversationName}" />

        <TextBlock Grid.Row="1" Text="Initiator Service: " />
        <ComboBox  Grid.Column="1" Grid.Row="1" SelectedItem="{Binding Path=Initiator, Mode=TwoWay}"
                    DisplayMemberPath="ServiceName"
                    ItemsSource="{Binding ElementName=root, Path=ServicesSource, Mode=OneWay}"
                    />

        <TextBlock Grid.Row="2" Text="Target Service: " />
        <ComboBox  Grid.Column="1" Grid.Row="2" SelectedItem="{Binding Path=Target, Mode=TwoWay}"
                    DisplayMemberPath="ServiceName"
                    ItemsSource="{Binding ElementName=root, Path=ServicesSource, Mode=OneWay}"
                    />
    </Grid>
</UserControl>
