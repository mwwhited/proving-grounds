<UserControl x:Class="Ssb.Modeling.Wpf.EditorControls.MessageTypeEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="root"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignData Source=/DesignerData/MessageTypeSample.xaml}"
             d:XmlSchemaCollectionsSource="{d:DesignData Source=/DesignerData/MessageTypeSample.xaml}"
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Icons.xaml" />
                <ResourceDictionary Source="../Resources/EnumLists.xaml" />
                <ResourceDictionary Source="../Resources/TypeConverters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Message Type Name: " />
        <TextBox Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Path=MessageTypeName}" />

        <TextBlock Grid.Row="1" Text="Validation: " />
        <ComboBox Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="1" 
                                              SelectedItem="{Binding Path=Validation, Mode=TwoWay}"
                                              ItemsSource="{Binding Source={StaticResource validationType}, Mode=OneWay}"
                                              />


        <TextBlock Grid.Row="2" Text="Xml Schema Collection: " />
        <ComboBox Grid.Row="2" Grid.Column="1" 
                SelectedItem="{Binding Path=XmlSchemaCollection, Mode=TwoWay}"
                DisplayMemberPath="DisplayName"
                ItemsSource="{Binding ElementName=root,Path=XmlSchemaCollectionsSource, Mode=OneWay}"
                />
        <Button Grid.Row="2" Grid.Column="2" Tag="Clear" 
                Command="{Binding Path=ClearXmlSchemaCollection}"
                Height="24"
                >
            <Image Source="{StaticResource icon-action-delete-small}" />
        </Button>
    </Grid>
</UserControl>
